# Repository audit fixes

This change addresses the repository audit of `bb26ee5`. It uses one quote response contract and the server recommendation throughout the API and frontend.

| Findings                                | Change                                                                                                                                                                                         | Regression evidence                                                                         |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| P1: wrong sender and wrong chain        | Preview quotes have no execution data. Executable quotes bind the sender. Wallet actions check the current form, quote, provider, account, and chain before submission and after confirmation. | `server.quote-compare.test.ts`, `transactionStore.test.ts`, `CompareForm.test.ts`           |
| P1: missing or invalid Curve setup      | Register Curve independently of optional provider keys and supply `getRpcUrl`. Restore the adapter on upstream Spandex 0.11.0.                                                                 | `config.test.ts`, fresh Git-package import, live Ethereum quotes                            |
| P1: public credential exposure          | Return fixed public errors. Remove credentials before logs, stored insights, and Sentry. Preserve full addresses and transaction hashes.                                                       | `redaction.test.ts`, `server.branches.test.ts`, `server.quote-compare.test.ts`              |
| Malformed Host crash                    | Validate the authority and request target, use a fixed URL base, and catch handler failures.                                                                                                   | Malformed Host requests followed by successful health requests                              |
| Curve slippage mismatch                 | Pass the requested basis points through the API and convert them to the SDK percentage unit.                                                                                                   | Both quote modes at 0, 10, 50, and 100 bps; decoded minimum-output assertions               |
| Stale form inputs and refresh           | Invalidate quotes for every input and wallet change. Stop the old refresh and resolve selected-chain metadata before comparing.                                                                | Mounted form tests for chain, slippage, account, wallet chain, and refresh timing           |
| Approval state reuse                    | Store allowance by chain, account, token, and spender; compare it with each quote's required input amount.                                                                                     | Token, spender, account, chain, amount, and delayed allowance-read tests                    |
| Canceled request race                   | One request sequence guards response data, errors, and loading cleanup.                                                                                                                        | Old resolve/reject results cannot alter a newer request                                     |
| Token-list startup overwrite            | Load saved identities before network work. Update entries by URL and preserve disabled or unavailable lists.                                                                                   | Delayed custom list, disabled Uniswap, removal, and request failure tests                   |
| Assumed token decimals                  | Use unresolved metadata state, share pending lookups, and reject late results for old selections. Do not invent 18 decimals.                                                                   | App startup in both load orders, URL/default selections, zero decimals, failed lookup retry |
| Browser ignores gas comparison          | Fetch `/compare` once and display its recommendation.                                                                                                                                          | A lower raw-output quote wins when the server includes gas                                  |
| Wrong gas assets and units              | Use canonical wrapped native addresses, exact gas prices, and integer conversion arithmetic. Report the comparison basis.                                                                      | Seven-chain address/currency table, both modes, missing gas/rate data, sub-wei ranking      |
| Compose route mismatch                  | Both Compose files route `/api` and `/api/…` through the same strip-prefix middleware. Preserve the standard Farcaster manifest path.                                                          | Compose configuration and container smoke checks                                            |
| Missing container RPC settings          | Forward all seven per-chain RPC overrides.                                                                                                                                                     | Both Compose configurations                                                                 |
| Local port mismatch                     | Use 3100 by default and make Vite honor the root `.env` or process `PORT`.                                                                                                                     | Type checks and configured proxy inspection                                                 |
| Response/schema mismatch                | Define the shared quote shape in Zod and regenerate OpenAPI client types. Preserve route and approval data.                                                                                    | Actual HTTP responses parsed with the shared schemas; quote-detail component tests          |
| Saved MEV rejection fallback            | Remove app signing and saved MEV settings. Submit through the connected wallet RPC and stop after rejection.                                                                                   | One wallet prompt after code 4001; no signing or raw-transaction method                     |
| Zero-decimal balances                   | Scale with BigInt and cache raw balances. Ignore late balance responses.                                                                                                                       | Decimal counts 0, 6, 18, and 255; cache reformatting and account-change tests               |
| Successful quote with failed simulation | Require both quote success and simulation success before selection or execution.                                                                                                               | Failed simulation fixtures cannot win or expose execution data                              |

The Spandex fork also retries failed SDK initialization, isolates SDK instances by chain and RPC URL, and always returns the ERC-20 approval identity. The fork review is [SatoshiAndKin/spandex#2](https://github.com/SatoshiAndKin/spandex/pull/2). The app pins commit `04db5f3d814ad55fc740ef519ec347c9c6cec165`.

Dependency updates retain the seven-day release age policy and strict build allowlist. The runtime/build baseline is Node 24.21.0, pnpm 12.3.1, and Bun 1.4.0. TypeScript versions follow the supported peer ranges of the Svelte, ESLint, and OpenAPI tools. The `js-yaml` override replaces the generator's vulnerable 4.3.1 pin with 4.3.2. CI now fails on high or critical dependency advisories.

Live validation uses the supplied Ethereum node with HTTPS and confirms chain ID 1. The private endpoint does not enter tracked defaults. No signed swaps or production deployments run as part of these checks.

A cold Curve request can exceed the existing five-second provider deadline. The next refresh uses the initialized SDK. Missing conversion data produces a stated raw-amount comparison. `targetOut` estimates required input and output; it does not promise exact-output settlement.

The fork's full upstream test run has four failures in unchanged Base RPC/anvil cases: 194 pass and six skip. The supplied Ethereum endpoint cannot serve Base state. Focused fork tests and Ethereum quote checks pass.

Validation on the final application code:

- `just validate` passes type checks, lint, formatting, 197 API tests, 402 frontend tests, dead-code checks, duplicate checks, dead-flag checks, and both Docker image builds.
- API coverage passes its thresholds: 96.76% lines, 90.84% branches, and 92.94% functions. Frontend line coverage increases from 74.07% to 82.78%.
- The dependency audit reports no known vulnerabilities. Fresh and cached frozen-lockfile Git dependency installs pass.
- Eight live Ethereum calldata checks cover both quote modes at 0, 10, 50, and 100 bps. Actual HTTP comparison responses pass the shared schemas.
- Both Compose route configurations and all seven RPC overrides pass the container checks. The API starts with networking disabled. API routes, documentation, the Farcaster manifest, and frontend fallback routes return the expected content.

The API container starts Node directly. This prevents pnpm 12 from trying to install workspace packages at container startup. The final API image rebuild and offline startup check pass after this change.
